#!/bin/bash

: "${DOGBOT_PATH:=/workspaces/DogBot}"

if [[ -z "${DOGBOT_PATH}" || "${DOGBOT_PATH}" == "/" ]]; then
    echo "Invalid DOGBOT_PATH: '${DOGBOT_PATH}'"
    exit 1
fi

sudo rm -rf -- \
    "${DOGBOT_PATH}/ros2_ws/build-cross-arm64" \
    "${DOGBOT_PATH}/ros2_ws/install-cross-arm64" \
    "${DOGBOT_PATH}/ros2_ws/log-cross-arm64"
